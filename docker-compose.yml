version: '3.10'

services:
  web:
    build: .
    env_file:
      - .env
    volumes:
      - ./:/usr/src/app
    ports:
      - 8000:8000
    depends_on:
      - db
  db:
    image: postgres:13
    volumes:
      - ./data/postgresql:/var/lib/postgresql/data/
    ports:
      - 8001:5432
    env_file:
      - .env